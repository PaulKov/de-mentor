# Deprecated. Use labs/greenplum-625/docker-compose.yml
# CLI lab names `greenplum` and `greenplum-625` both point there (port 15436).
services:
  greenplum-deprecated-pointer:
    image: alpine:3.20
    profiles: ["never"]
    command: ["true"]
